{
  "name": "data",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --port 3000 --host 0.0.0.0",
    "build": "vite build",
    "preview": "vite preview --port 3000 --host 0.0.0.0"
  },
  "devDependencies": {
    "vite": "^6.0.5"
  },
  "dependencies": {
    "@sentry/browser": "^8.47.0",
    "ammo.js": "^0.0.10",
    "three": "^0.172.0"
  }
}
